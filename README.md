# EchoService

Mid-tier API for frontend data interactions.

## Run locally

```bash
npm install
npm start
```

## Docker

```bash
docker compose up -d --build
```

## Endpoints

- `GET /health`
- `GET /api/ping`
- `POST /api/echo`
